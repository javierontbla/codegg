import React from "react";

import { TitleContainer } from "./Title_styles";

const Title = ({ title }) => {
  return (
    <>
      <TitleContainer>{title}</TitleContainer>
    </>
  );
};

export default Title;
